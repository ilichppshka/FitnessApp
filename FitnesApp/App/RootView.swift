import SwiftUI

struct RootView: View {
    var body: some View {
        Text("FitnesApp")
            .foregroundStyle(.white)
    }
}

#Preview {
    RootView()
        .preferredColorScheme(.dark)
}
